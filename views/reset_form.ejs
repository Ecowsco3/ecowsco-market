<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Reset Password - Ecowsco Market</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen flex items-center justify-center px-4">
  <div class="bg-white p-8 rounded-2xl shadow-xl w-full max-w-md">
    <div class="text-center mb-6">
      <h2 class="text-3xl font-bold text-gray-800">Reset Your Password</h2>
      <p class="text-gray-600 mt-2">Enter your new password below</p>
    </div>
    
    <form action="/reset-password/<%= token %>" method="POST" class="space-y-5">
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">New Password</label>
        <input type="password" name="password" placeholder="••••••••" required 
               class="w-full border-2 border-gray-200 px-4 py-3 rounded-lg focus:border-blue-500 focus:outline-none transition">
        <p class="text-xs text-gray-500 mt-1">Choose a strong password</p>
      </div>
      
      <button type="submit" 
              class="w-full bg-gradient-to-r from-green-500 to-emerald-600 text-white px-6 py-3 rounded-lg font-semibold hover:from-green-600 hover:to-emerald-700 transition shadow-lg">
        Reset Password
      </button>
    </form>
    
    <p class="mt-6 text-center text-sm text-gray-600">
      <a href="/login" class="text-blue-600 hover:text-blue-800 font-medium">← Back to Login</a>
    </p>
  </div>
</body>
</html>
