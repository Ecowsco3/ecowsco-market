<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Password Reset</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded shadow-md w-full max-w-md">
    <h2 class="text-2xl font-bold mb-4">Password Reset</h2>
    
    <% if (resetLink) { %>
      <div class="bg-green-50 border border-green-200 rounded p-4 mb-4">
        <p class="text-green-800 font-semibold mb-2">Your password reset link:</p>
        <a href="<%= resetLink %>" class="text-blue-600 hover:text-blue-800 break-all underline"><%= resetLink %></a>
        <p class="text-sm text-gray-600 mt-3">Click the link above to reset your password. This link will expire in 1 hour.</p>
      </div>
      <a href="/reset-request" class="block text-center bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 w-full">Request Another Link</a>
    <% } else { %>
      <form action="/reset-request" method="POST" class="space-y-4">
        <input type="email" name="email" placeholder="Enter your email" required class="w-full border px-3 py-2 rounded">
        <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 w-full">Get Reset Link</button>
      </form>
    <% } %>
    
    <p class="mt-2 text-sm"><a href="/login" class="text-blue-500">Back to Login</a></p>
  </div>
</body>
</html>