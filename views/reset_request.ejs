<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Password Reset</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center px-4 sm:px-6">
  <div class="bg-white p-6 sm:p-8 rounded-2xl shadow-md w-full max-w-md">
    <h2 class="text-xl sm:text-2xl font-bold mb-4 text-center text-gray-800">Password Reset</h2>
    
    <% if (resetLink) { %>
      <div class="bg-green-50 border border-green-200 rounded-xl p-4 sm:p-5 mb-5 text-center">
        <p class="text-green-800 font-semibold mb-2">Your password reset link:</p>
        <a href="<%= resetLink %>" class="text-blue-600 hover:text-blue-800 break-all underline text-sm sm:text-base"><%= resetLink %></a>
        <p class="text-xs sm:text-sm text-gray-600 mt-3">Click the link above to reset your password. This link will expire in 1 hour.</p>
      </div>
      
      <a href="/reset-request" 
         class="block text-center bg-gray-500 text-white px-4 py-2.5 rounded-lg hover:bg-gray-600 transition w-full text-sm sm:text-base font-medium">
        Request Another Link
      </a>
    <% } else { %>
      <form action="/reset-request" method="POST" class="space-y-4">
        <div>
          <input type="email" 
                 name="email" 
                 placeholder="Enter your email" 
                 required 
                 class="w-full border border-gray-300 px-4 py-3 rounded-lg focus:ring-2 focus:ring-blue-400 focus:outline-none text-sm sm:text-base transition">
        </div>
        <button type="submit" 
                class="bg-blue-500 text-white px-4 py-3 rounded-lg hover:bg-blue-600 transition w-full text-sm sm:text-base font-semibold shadow">
          Get Reset Link
        </button>
      </form>
    <% } %>
    
    <p class="mt-4 text-center text-sm text-gray-600">
      <a href="/login" class="text-blue-500 hover:text-blue-700 font-medium">← Back to Login</a>
    </p>
  </div>
</body>
    </html>