<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Dashboard - Ecowsco Market</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
  <nav class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
      <div>
        <h1 class="text-2xl font-bold text-gray-800">Ecowsco Market</h1>
        <p class="text-sm text-gray-600">Welcome, <%= vendor.name %></p>
      </div>
      <a href="/logout" class="bg-red-500 text-white px-5 py-2 rounded-lg hover:bg-red-600 transition font-medium">
        Logout
      </a>
    </div>
  </nav>

  <div class="max-w-7xl mx-auto px-6 py-8">
    <div class="mb-8">
      <a href="/add-product" class="inline-block bg-gradient-to-r from-green-500 to-emerald-600 text-white px-6 py-3 rounded-lg hover:from-green-600 hover:to-emerald-700 transition font-semibold shadow-lg">
        + Add New Product
      </a>
    </div>

    <div class="bg-white rounded-2xl shadow-xl p-6">
      <h2 class="text-2xl font-bold mb-6 text-gray-800">Your Products</h2>
      <% if(products.length===0){ %>
        <div class="text-center py-12">
          <svg class="w-16 h-16 mx-auto text-gray-300 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
          </svg>
          <p class="text-gray-500 text-lg">No products yet. Start by adding your first product!</p>
        </div>
      <% }else{ %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% products.forEach(p=>{ %>
            <div class="bg-white border-2 border-gray-100 rounded-xl shadow-md hover:shadow-xl transition overflow-hidden">
              <img src="<%= p.image %>" alt="<%= p.name %>" class="w-full h-48 object-cover">
              <div class="p-4">
                <h3 class="font-bold text-lg text-gray-800 mb-1"><%= p.name %></h3>
                <p class="text-2xl font-bold text-green-600 mb-2">₵<%= p.price %></p>
                <p class="text-sm text-gray-600"><%= p.description || 'No description' %></p>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>